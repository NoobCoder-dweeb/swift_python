# Docker-specific environment variables for docker-compose
# These variables are used when running the application with Docker Compose

# PostgreSQL service configuration
POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=

# Application configuration for Docker environment
DATABASE_URL=
SWIFT_STORAGE_BACKEND=
SWIFT_CREWAI_ENABLED=
SWIFT_SEED_DEMO_DATA=
